import 'package:flutter/material.dart';

class ItemBlock extends StatelessWidget {
  const ItemBlock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
